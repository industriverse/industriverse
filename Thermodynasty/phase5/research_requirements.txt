# Research Integration Dependencies for Phase 5 EIL
# Install: pip install -r research_requirements.txt

# === LeJEPA & Self-Supervised Learning ===
# JAX/Flax (already in base requirements)
jax>=0.4.20
flax>=0.7.5
optax>=0.1.7

# === Egocentric-10K & Dataset Handling ===
datasets>=2.16.0  # HuggingFace datasets for streaming
transformers>=4.36.0  # For model integration
torch>=2.1.0  # PyTorch for some models
torchvision>=0.16.0

# === PhysWorld & 3D Reconstruction ===
opencv-python>=4.8.0  # Video processing and optical flow
trimesh>=4.0.0  # 3D mesh processing
# open3d>=0.18.0  # Optional: 3D visualization (not available for Python 3.13 yet)
# pytorch3d>=0.7.5  # Optional: Advanced 3D operations (requires manual install)

# === RealDeepResearch & Paper Crawling ===
requests>=2.31.0  # HTTP requests for ArXiv API
beautifulsoup4>=4.12.0  # HTML parsing
arxiv>=2.1.0  # ArXiv API wrapper

# Embeddings (optional but recommended)
sentence-transformers>=2.2.0  # Better text embeddings than TF-IDF
scikit-learn>=1.3.0  # Clustering and ML utilities

# Vector DB for knowledge graphs (optional)
# chromadb>=0.4.0  # Vector database for embeddings
# faiss-cpu>=1.7.4  # Fast similarity search

# === General Utilities ===
numpy>=1.24.0  # Already in base
scipy>=1.11.0  # Scientific computing

# === Testing & Development ===
pytest>=7.4.0  # Already in base
pytest-asyncio>=0.21.0

# === Monitoring (optional) ===
wandb>=0.16.0  # Experiment tracking for pretraining
tensorboard>=2.15.0  # TensorBoard logging

# === Notes ===
# - Some packages have large dependencies (torch, open3d)
# - pytorch3d requires manual install: https://github.com/facebookresearch/pytorch3d/blob/main/INSTALL.md
# - For full Egocentric-10K: Ensure 20TB+ storage available
# - For LeJEPA pretraining: Recommended 8x A100 GPUs (can start with smaller models)
