apiVersion: apps/v1
kind: Deployment
metadata:
  name: resource-reservation-optimized
  namespace: obmi-quantum-enhancement
spec:
  replicas: 1
  selector:
    matchLabels:
      app: resource-reservation-optimized
  template:
    metadata:
      labels:
        app: resource-reservation-optimized
    spec:
      nodeSelector:
        workload-type: general
      containers:
      - name: placeholder
        image: busybox
        command: ["sleep", "infinity"]
        resources:
          requests:
            cpu: 200m
            memory: 512Mi
          limits:
            cpu: 500m
            memory: 1Gi
