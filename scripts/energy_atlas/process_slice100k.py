import os
import sys

def process_slice100k(dataset_path: str, output_path: str):
    """
    Stub for Slice100k G-code processing.
    Will be implemented when drive is connected.
    """
    print(f"Ready to process Slice100k from {dataset_path}")
    print("Logic: G-code -> Energy Voxels")

if __name__ == "__main__":
    print("Waiting for external drive...")
