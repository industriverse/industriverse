apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- aws/trifecta-deployment.yaml
- aws/bridge-api-service.yaml

# Apply common labels
commonLabels:
  environment: production
  managed-by: industriverse-operator

# Global patches could go here
# patches:
# - path: sidecars/ai-shield-sidecar.yaml
#   target:
#     kind: Deployment
