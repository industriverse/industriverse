apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: protocol-layer-data-pvc
  namespace: industriverse-protocol
  labels:
    app: industriverse-protocol-layer
    component: protocol-layer
    part-of: industriverse
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard
